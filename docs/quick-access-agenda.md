# Quick Access Agenda

Quick add is now a dedicated in-app flow. The remaining quick-access work is intentionally separate because it needs native build testing.

## Notifications

- Add notification permission flow.
- Add a local reminder notification for daily/weekly expense logging.
- Add a notification action that opens Quick Add.
- Test behavior on the preview APK, not Expo Go.

## Home Screen Widget

- Confirm whether the widget should show balance, monthly spending, or both.
- Evaluate Expo-compatible widget approaches versus a custom native module.
- Build a simple Android widget only after the preview APK path is stable.
- Use the widget button to deep-link into Quick Add.
